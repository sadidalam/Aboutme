<html lang="en"><head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Who I Am — Sadid</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&amp;family=Playfair+Display:wght@700&amp;display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #0f0f12;
      --text: #eaeaf0;
      --muted: #b6b7c2;
      --accent: #c9a227;
      --accent-2: #4cc9f0;
    }

    body {
      margin: 0;
      font-family: "Poppins", sans-serif;
      background: radial-gradient(1200px 600px at 20% 10%, #14151a 0%, var(--bg) 60%);
      color: var(--text);
      padding: 40px 20px;
      line-height: 1.6;
      animation: fadeIn 1.5s ease-out;
    }

    h1 {
      font-family: "Playfair Display", serif;
      font-size: 42px;
      text-align: center;
      margin-bottom: 10px;
      animation: fadeInUp 1s ease-out;
    }

    h2 {
      font-size: 18px;
      text-align: center;
      color: var(--accent);
      margin-bottom: 30px;
      animation: fadeIn 2s ease-out;
    }

    .intro {
      max-width: 700px;
      margin: 0 auto;
      font-size: 16px;
      color: var(--muted);
      animation: fadeIn 2s ease-out;
    }

    .countdown {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin: 30px 0;
      flex-wrap: wrap;
    }

    .count-box {
      background: #1a1b21;
      border-radius: 14px;
      padding: 14px;
      text-align: center;
      box-shadow: 0 8px 24px rgba(0,0,0,0.25);
      width: 100px;
    }

    .count-box .num {
      font-size: 26px;
      font-weight: 700;
      color: var(--accent);
    }

    .count-box .label {
      font-size: 12px;
      color: var(--muted);
      text-transform: uppercase;
    }

    .social {
      text-align: center;
      margin-top: 40px;
      font-size: 15px;
    }

    .social a {
      color: var(--accent-2);
      text-decoration: none;
      margin: 0 10px;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
  </style>
</head>
<body>
  <h1>Hello</h1>
  <h2>INTRODUCING ME!</h2>

  <div class="intro">
    Hi, I'm Sadid (pronounced Shadid). People often call me Sajid, Sadad, or Sadik, but my real name is Sadid. My birthday is on February 19, so remember—you owe me at least one luxurious gift on that day! And don't try to give me a treat back, because I don't like that. I absolutely love chocolate, so you already know what to get. My favorite car brand is BMW, though I also admire Rolls Royce, which belongs to the same company. My favorite game is Minecraft, though I just played it a few days ago. When it comes to food, I’ll enjoy almost anything you treat me—except Fuchka, Chatpati, Jhalmuri, or anything toxic (sounds funny, but it’s true!). That’s me in a nutshell… just kidding, but also kind of real!<br><br><br>
Upcoming Birthday countdown:
  </div>

  <div class="countdown" id="countdown">
    <div class="count-box">
      <div class="num" id="days">35</div>
      <div class="label">Days</div>
    </div>
    <div class="count-box">
      <div class="num" id="hours">3</div>
      <div class="label">Hours</div>
    </div>
    <div class="count-box">
      <div class="num" id="minutes">15</div>
      <div class="label">Minutes</div>
    </div>
    <div class="count-box">
      <div class="num" id="seconds">7</div>
      <div class="label">Seconds</div>
    </div>
  </div>

  <div class="social">
    <p>Instagram:</p>
    <a href="https://instagram.com/shadidsadid">@shadidsadid</a>
    <a href="https://instagram.com/sadidalam19">@sadidalam19</a>
  </div>

  <script>
    function nextBirthday(month = 2, day = 19) {
      const now = new Date();
      let target = new Date(now.getFullYear(), month - 1, day);
      if (target < now) target = new Date(now.getFullYear() + 1, month - 1, day);
      return target;
    }

    const targetDate = nextBirthday(2, 19);

    function updateCountdown() {
      const now = new Date();
      const diff = targetDate - now;

      const seconds = Math.floor(diff / 1000) % 60;
      const minutes = Math.floor(diff / (1000 * 60)) % 60;
      const hours = Math.floor(diff / (1000 * 60 * 60)) % 24;
      const days = Math.floor(diff / (1000 * 60 * 60 * 24));

      document.getElementById('days').textContent = days;
      document.getElementById('hours').textContent = hours;
      document.getElementById('minutes').textContent = minutes;
      document.getElementById('seconds').textContent = seconds;
    }

    updateCountdown();
    setInterval(updateCountdown, 1000);
  </script>

</body></html>
